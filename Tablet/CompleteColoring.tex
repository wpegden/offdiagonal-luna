\begin{definition}
An $\ell$-coloring of the edges of the complete graph on $n$ labelled
vertices is represented by a symmetric map
$c:[n]\times[n]\to[\ell]$.  Values on the diagonal are harmless auxiliary
values; the color of an edge is read only on pairs of distinct vertices.
\end{definition}
