\begin{definition}
For a loop-aware graph $G$ and an integer $k$, an independent-set witness of
order $k$ is an injective map from $[k]$ to the vertex set such that no two
distinct image vertices are adjacent.  Adjacency on the diagonal is irrelevant
to this witness, as in the paper's use of graphs that may have loops.
\end{definition}
